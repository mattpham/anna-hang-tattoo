import styled from 'styled-components';

const Header = styled.header`
  background: none;
  padding: 1rem;
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1;
`;

export default Header;
